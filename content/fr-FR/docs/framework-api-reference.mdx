import ApiDocs from "@/components/api-docs";

# Référence de l'API du framework

Cette page intègre l'index d'API généré depuis `openmrs-esm-core`, dans `packages/framework/esm-framework/docs/API.md`. Les entrées d'API liées sont produites à partir des commentaires du code source du framework et s'ouvrent sur GitHub.

<ApiDocs />
