import ApiDocs from "@/components/api-docs";

# Framework API reference

This page embeds the generated API index from `openmrs-esm-core` at `packages/framework/esm-framework/docs/API.md`. The linked API entries are produced from comments in the framework source code and open on GitHub.

<ApiDocs />
